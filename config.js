var GAME = {
  canvas : {
    width : 600,
    height : 300
  },
  started : true,
  level : 1
};

var BLOOD = {
  collectedDrops : 0
};

var COORD = {
  x1 : Math.random()*600,
  y1 : Math.random()*300,
  x2 : Math.random()*600,
  y2 : Math.random()*300,
  x3 : Math.random()*600,
  y3 : Math.random()*300,
  x4 : Math.random()*600,
  y4 : Math.random()*300,
  x5 : Math.random()*600,
  y5 : Math.random()*300,
  x6 : Math.random()*600,
  y6 : Math.random()*300
};
